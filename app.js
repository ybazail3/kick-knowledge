function lessons(lesson) {
    return 
    ` <h2 class="row-title">TODO: add new title here</h2>
    <h3>Question TODO:${}</h3>
    <p>${}</p>
      <input type="checkbox" id="choice1" name="choice1" value="${}">
      <label for="choice1"> ${}</label><br>
      <input type="checkbox" id="choice2" name="choice2" value="${}">
      <label for="choice2"> ${}</label><br>
      <input type="checkbox" id="choice3" name="choice3" value="${}">
      <label for="choice3"> ${}</label><br>
      <input type="checkbox" id="choice3" name="choice3" value="${}">
      <label for="choice3"> ${}</label><br>`;
}

// Add a switch statement that will go through all the lessons/questions
// Needs to track if the questions are correct or incorrect



// If questions are correct move to the next screen, next screen shows the 3 games offered
// Once the user chooses to play a game set a timer for 1min 30sec, once timer is over 

